# Backend base URL. Leave unset to configure it in the UI at runtime.
VITE_API_BASE_URL=http://localhost:3000

# Optional local convenience: pre-fills the device token field. Put the real
# value in .env.local, which is gitignored. Never commit a token.
VITE_DEVICE_TOKEN=
